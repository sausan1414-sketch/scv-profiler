#CLI + Streamlit simple CSV profiler example project

This is a simple example project for teaching Python.
It demonstrates:
- A Typer-based CLI
- Basic CSV profiling
- Optional Streamlit web interface

##Run CSV profiling from CLI
```sh
uvx git+https://github.com/FarisHijazi/csv-profiler.git csv-profiler profile data/sample.csv --out-dir outputs --format both
